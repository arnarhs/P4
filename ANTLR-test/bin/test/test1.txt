//Test case that doesn't work

species a: 10

species b: 10

//Define your solution object. 

solution test(species a, species b) {

	//TODO: Maybe consider whether we want this behavior, and if we want it, what that will mean for our "Reaction" type
	reaction one: a + b => c + d (k) => a * b (k) => 2*c (k)
	//TODO: FIX
	reaction two: 2 => 2

	reaction three: a + b => c + d (k) }

//ssa ssaModel: ({a, b, c}, rSet)
//simulation sim: ssaModel.simulate(generations)
//list<simulations> sim: ssaModel.simulate(generations, trajectories)
a(a , a ,a)
